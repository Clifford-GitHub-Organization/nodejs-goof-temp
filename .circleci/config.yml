version: 2.1

orbs:
  snyk: snyk/snyk@2.2.0

jobs:
 build:
   docker:
     - image: circleci/node:latest
   steps:
     - checkout
     - run: npm install -q
     - snyk/scan:
          token-variable: SNYK_TOKEN
          fail-on-issues: false